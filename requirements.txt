# FastAPI Backend Dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.18

# Database
# sqlite3 is built into Python standard library (no pip install needed)

# PDF Generation (for future reports)
weasyprint==60.2
jinja2==3.1.6

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0  # For testing

# Authentication (migrated from python-jose due to CVE-2024-33663)
# PyJWT is the recommended secure alternative to python-jose
pyjwt[crypto]==2.8.0  # For future auth - secure JWT implementation
passlib[bcrypt]==1.7.4  # For future auth
