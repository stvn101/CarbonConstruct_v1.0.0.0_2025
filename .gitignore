cat << 'EOF' > .gitignore
# =================================================================
# 🇦🇺 STRICT AUSTRALIAN/EU SAAS COMPLIANCE .GITIGNORE
# =================================================================

# --- 1. SENSITIVE CREDENTIALS & SECRETS ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging.local
.env*.local
*.pem
*.key
*.pub
*.pfx
*.p12
*.crt
*.der
service-account*.json
*-sa-*.json
firebase-adminsdk*.json
config/secrets.*
credentials.json

# --- 2. LOCAL DATA & PII (Privacy Act / GDPR Protection) ---
*.db
*.sqlite
*.sql
*.dump
*.bin
/data/
/db/
/backups/
/temp/
/tmp/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/logs/

# --- 3. DEPENDENCIES & BUILD ---
node_modules/
jspm_packages/
venv/
.venv/
env/
bin/
obj/
lib/
dist/
out/
build/
.next/
.serverless/
.terraform/

# --- 4. OS & IDE METADATA ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
.project
.settings/
*.sublime-project
*.sublime-workspace

# --- 5. FINANCIAL & TAX CACHE ---
.sass-cache/
.cache/
.parcel-cache/
.turbo/

# --- 6. TEST COVERAGE & COMPLIANCE REPORTS ---
coverage/
.nyc_output/
htmlcov/
.pytest_cache/
__pycache__/
junit.xml
test-results/

# --- 7. DANGEROUS SYSTEM FILES ---
core
*.exe
*.dll
*.so
*.dylib
EOF

# --- PURGE PREVIOUSLY TRACKED SECRETS ---
git rm -r --cached .env* *.pem *.key *.sql 2>/dev/null

git add .gitignore

git commit -m "🔒 SECURITY: Enforced 80-line compliance .gitignore and purged secret cache"

echo "✅ .gitignore is now STRICT and compliant."